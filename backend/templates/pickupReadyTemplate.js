export const getPickupReadyMessage = (customerName, orderId) => {
  return `Namaste ${customerName}, your bridal set for order #${orderId} is steam-cleaned, inspected, and ready at our FC Road studio.`;
};
