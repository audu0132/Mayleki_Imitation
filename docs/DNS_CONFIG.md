# Mayleki Domain & DNS Configuration

### Production Records
- `A` record: `@` points to Render / Vercel Edge Server.
- `CNAME` record: `www` points to `mayleki.com`.
- `TXT` record: SPF and DKIM for concierge email deliverability.
